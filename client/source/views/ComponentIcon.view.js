import Preact from "preact"

import "views/ComponentIcon.view.less"

export default class ComponentIcon {
    render() {
        return (
            <div className="ComponentIcon"/>
        )
    }
}
